---
title: "9. S3"
type: docs
weight: 1
---

## S3 (Simple Storage Service)

Amazon S3 is an object storage service that lets you store and retrieve any amount of data from anywhere on the internet. Unlike a file system (which organises data in a folder hierarchy) or a block storage volume (which behaves like a hard disk), S3 stores data as discrete **objects** — each one a self-contained bundle of data, metadata, and a unique key. S3 exists because applications need a place to persist files that is durable, infinitely scalable, and decoupled from any single server. It underpins a huge slice of AWS architectures: application assets, data lakes, backups, static sites, event pipelines, and more. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/Welcome.html)

### Buckets, Objects, Keys, and Regions

Every object in S3 lives inside a **bucket**. A bucket is a named container that belongs to one AWS region. You choose the region at creation time and it never changes, which matters for latency, compliance, and replication. Bucket names must be globally unique across all AWS accounts — if someone already owns `my-app-assets`, you cannot use it.

Inside a bucket, each object is identified by its **key** — essentially the object's full path within the bucket (e.g., `images/profile/user-42.jpg`). S3 has no real folder hierarchy; the slashes in a key are just characters that the console renders as folders for convenience. The combination of bucket name + key + (optionally) version ID uniquely identifies every object in S3. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/UsingObjects.html)

A single object can be up to **5 TB** in size. Each object also carries **metadata** — system metadata (content type, size, ETag) and optional user-defined key-value pairs you attach at upload time.

### Storage Classes

S3 offers several storage classes tuned for different access patterns and cost profiles. Choosing the right class is one of the most cost-effective decisions you can make. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/storage-class-intro.html)

- **S3 Standard** — The default. High durability (11 nines), high availability, low latency. Use for frequently accessed data.
- **S3 Standard-IA** (Infrequent Access) — Same durability and low latency as Standard but cheaper storage cost, with a per-retrieval fee. Use for data accessed less than once a month (e.g., backups, disaster recovery files).
- **S3 One Zone-IA** — Like Standard-IA but stored in a single AZ. Cheaper still, but you lose resilience to AZ failure. Acceptable for reproducible data (e.g., thumbnail images that can be regenerated).
- **S3 Glacier Instant Retrieval** — Archive tier with millisecond retrieval. Designed for data accessed roughly once a quarter but needing immediate access when requested (e.g., medical images, media archives).
- **S3 Glacier Flexible Retrieval** — Archive tier with retrieval times of minutes to hours (Expedited, Standard, Bulk). Lower cost than Glacier Instant. Use when retrieval speed is not time-critical.
- **S3 Glacier Deep Archive** — The lowest-cost tier. Retrieval takes 12–48 hours. Designed for long-term regulatory retention (e.g., 7-year compliance archives).
- **S3 Intelligent-Tiering** — Monitors access patterns and automatically moves objects between access tiers (Frequent, Infrequent, Archive Instant, Archive, Deep Archive). Small monthly monitoring fee per object. Ideal when access patterns are unknown or unpredictable.

### Lifecycle Policies

Rather than manually managing storage classes, you can define **lifecycle policies** — rules that S3 evaluates daily to automatically transition or expire objects. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/object-lifecycle-mgmt.html)

A typical rule might say: "After 30 days, transition objects under the `logs/` prefix to Standard-IA. After 90 days, transition to Glacier Flexible Retrieval. After 365 days, delete them." This keeps storage costs low without any application-side logic. Transitions can only move objects **down** the cost hierarchy (you cannot transition from Glacier back to Standard via a lifecycle rule; that requires a restore operation).

### Versioning and MFA Delete

When **versioning** is enabled on a bucket, S3 retains every version of every object instead of overwriting it. Each upload creates a new version with a unique version ID. A `DELETE` request on a versioned object does not erase it — it places a **delete marker** on top, making the object appear deleted while all prior versions remain intact and recoverable. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/Versioning.html)

Versioning is a prerequisite for S3 Replication and Object Lock.

**MFA Delete** adds a second layer of protection: in addition to valid AWS credentials, permanently deleting a version or disabling versioning requires a valid MFA token. This protects against both accidental deletion and compromised credentials. MFA Delete can only be enabled by the bucket owner using the root account via the CLI. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/MultiFactorAuthenticationDelete.html)

### Static Website Hosting

S3 can serve a bucket's contents as a static website — HTML, CSS, JavaScript, images — directly over HTTP, without any web server. You enable the feature on the bucket, specify an index document (e.g., `index.html`) and optionally an error document, and S3 exposes a public endpoint like `http://my-bucket.s3-website.eu-west-1.amazonaws.com`. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/WebsiteHosting.html)

A common pattern is to front this endpoint with **CloudFront** for HTTPS, global caching, and a custom domain. For a React or Vue single-page app, this is a completely serverless hosting solution with no EC2 required.

### S3 Replication (CRR and SRR)

S3 Replication automatically copies objects from a **source bucket** to a **destination bucket** asynchronously in the background. Versioning must be enabled on both buckets. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/replication.html)

- **Cross-Region Replication (CRR)** — Source and destination are in different AWS regions. Used for disaster recovery, latency reduction for geographically distributed users, or regulatory requirements to keep copies in specific regions.
- **Same-Region Replication (SRR)** — Source and destination are in the same region. Used for log aggregation across accounts, or maintaining a live copy in a separate account for compliance.

Replication copies only **new** objects written after the rule is created. Existing objects are not replicated automatically (you need S3 Batch Replication for that). Delete markers are not replicated by default, but this can be enabled explicitly.

### Pre-signed URLs (Downloads and Uploads)

By default, S3 objects in a private bucket are inaccessible to the public. A **pre-signed URL** grants time-limited access to a specific object without requiring the requester to have AWS credentials. The URL embeds the bucket owner's credentials and an expiry timestamp — anyone with the URL can use it until it expires. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/ShareObjectPreSignedURL.html)

Pre-signed URLs are most commonly associated with **downloads** (`GET`), but they also support **uploads** (`PUT`). This is a frequent exam trap: you can generate a pre-signed URL that lets an end user upload a file directly to S3 from their browser, bypassing your backend server entirely. The backend generates the URL, hands it to the client, and the client uploads directly to S3. This offloads upload bandwidth and processing from your application servers.

```python
# Generate a pre-signed URL for a PUT (upload) — valid for 1 hour
import boto3
s3 = boto3.client('s3', region_name='eu-west-1')

url = s3.generate_presigned_url(
    ClientMethod='put_object',
    Params={'Bucket': 'my-bucket', 'Key': 'uploads/user-file.pdf'},
    ExpiresIn=3600
)
```

### S3 Access Points

**Access Points** are named network endpoints attached to a bucket, each with its own access policy. Instead of a single, increasingly complex bucket policy managing access for many teams or applications, you create one Access Point per use case (e.g., one for the analytics team, one for the ingest pipeline) and attach a focused policy to each. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/access-points.html)

Access Points can also be scoped to a **VPC**, ensuring that only traffic originating from within that VPC can use the endpoint — never traversing the public internet.

### Event Notifications

S3 can emit **event notifications** when things happen to objects in a bucket: uploads (`s3:ObjectCreated`), deletions (`s3:ObjectRemoved`), restore completions from Glacier, and more. These notifications can be delivered to three destinations: [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/NotificationHowTo.html)

- **Lambda** — Process the object immediately (e.g., resize an uploaded image, parse a CSV, index content).
- **SQS** — Queue the event for reliable downstream processing.
- **SNS** — Fan out the event to multiple subscribers simultaneously.

A typical real-world pattern: a user uploads a video to S3 → S3 sends an `ObjectCreated` notification to an SQS queue → a Lambda function polls the queue and kicks off a MediaConvert transcoding job.

### Object Lock and Glacier Vault Lock

**Object Lock** enforces a **Write Once, Read Many (WORM)** model — objects cannot be overwritten or deleted for a defined retention period. This is used for regulatory compliance (financial records, audit logs) where data immutability must be provable. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/object-lock.html)

Object Lock operates in two modes:
- **Governance mode** — Users with special IAM permissions can override or remove the lock. Useful for testing and internal controls.
- **Compliance mode** — Nobody — not even the root account — can delete or alter the object during the retention period. Strongest protection.

You can also set a **Legal Hold** on individual objects independently of any retention period, preventing deletion until the hold is explicitly removed.

**Glacier Vault Lock** is a similar concept but applies to Glacier Vaults specifically. Once a policy is locked, it becomes immutable and cannot be changed. [🔗](https://docs.aws.amazon.com/amazonglacier/latest/dev/vault-lock.html)

### Encryption

S3 offers multiple encryption options for data at rest. Understanding which key-management model each uses is important for the exam. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/UsingEncryption.html)

- **SSE-S3** (Server-Side Encryption with S3-Managed Keys) — S3 handles everything. Keys are managed by AWS entirely behind the scenes. No configuration required, enabled by default on all new buckets. Header: `x-amz-server-side-encryption: AES256`.
- **SSE-KMS** (Server-Side Encryption with AWS KMS) — Objects are encrypted using a KMS Customer Master Key (CMK). You get an audit trail in CloudTrail (every decryption is logged), and you control key rotation and access policies. Header: `x-amz-server-side-encryption: aws:kms`. Important caveat: heavy S3 traffic with SSE-KMS can hit KMS API rate limits, since every `GET` calls KMS to decrypt.
- **DSSE-KMS** (Dual-layer Server-Side Encryption with KMS) — Two independent layers of KMS encryption. Designed for workloads with strict regulatory requirements. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/UsingDSSEncryption.html)
- **SSE-C** (Server-Side Encryption with Customer-Provided Keys) — You supply the encryption key with every request; S3 performs the encryption but does not store your key. Requires HTTPS. Useful when you need full key custody.
- **Client-Side Encryption** — You encrypt the data before sending it to S3. S3 stores ciphertext and has no knowledge of your keys or plaintext. Maximum control, maximum responsibility.

For data **in transit**, S3 supports HTTPS (TLS) and you can enforce it via a bucket policy that denies requests where `aws:SecureTransport` is `false`.

### Bucket Policies vs ACLs

Access to S3 resources is controlled primarily through **bucket policies** — JSON-based IAM-style policies attached directly to the bucket. They can grant or deny access to principals (users, roles, other accounts, anonymous users) and can reference conditions like IP address, VPC, or whether MFA was used. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/bucket-policies.html)

**Access Control Lists (ACLs)** are a legacy mechanism that predates bucket policies. They are coarser-grained (canned ACLs like `public-read`, `private`) and are now generally discouraged. AWS recommends disabling ACLs (using **Bucket Owner Enforced** mode) and relying exclusively on bucket policies and IAM policies. The one context where ACLs still see use is granting access to a specific AWS account that you don't control — though cross-account bucket policies cover this more cleanly.

A simple rule for the exam: **prefer bucket policies** for bucket-level access control; use IAM policies for controlling what your own users and roles can do.

### CORS Configuration

**CORS (Cross-Origin Resource Sharing)** becomes relevant when a web application hosted on one domain (e.g., `app.example.com`) makes a JavaScript request directly to an S3 bucket on a different domain (e.g., `mybucket.s3.amazonaws.com`). Browsers block these cross-origin requests by default. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/cors.html)

You configure CORS on the S3 bucket by specifying which origins are allowed, which HTTP methods are permitted, and which headers are exposed. This is required for browser-based uploads via pre-signed URLs or any JavaScript that fetches assets from a different S3 bucket origin than the page itself.

### S3 Select and Glacier Select

Normally, to find a record in a large CSV or JSON file stored in S3, you'd download the entire object and filter it in your application. **S3 Select** lets you push a SQL-like query directly to S3, which filters and returns only the relevant subset of data — dramatically reducing transfer costs and latency. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/selecting-content-from-objects.html)

**Glacier Select** provides the same capability for objects stored in Glacier, running queries against archived data without a full restore.

Both work on CSV, JSON, and Parquet formats, optionally compressed with GZIP or BZIP2. For large-scale analytics you'd use Athena, but S3 Select is lighter-weight and requires no infrastructure setup.

### Performance

S3 can handle very high request rates, but knowing the right tools to push it further matters at scale. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/optimizing-performance.html)

**Multipart Upload** splits a large object into parts that are uploaded independently and in parallel, then S3 assembles them. AWS recommends multipart upload for objects over 100 MB and requires it for objects over 5 GB. It also provides resilience: if one part fails, only that part needs to be retried. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/mpuoverview.html)

**Transfer Acceleration** routes uploads through AWS's globally distributed **CloudFront edge locations** using the optimised AWS backbone network instead of the public internet. A user in Tokyo uploading to a bucket in `us-east-1` will see significantly faster throughput with Transfer Acceleration enabled. There is an additional per-GB cost. [🔗](https://docs.aws.amazon.com/AmazonS3/latest/userguide/transfer-acceleration.html)

**Byte-Range Fetches** allow you to download specific byte ranges of an object in parallel — analogous to multipart upload but for downloads. You split the target object into ranges, issue concurrent `GetObject` requests for each range, and reconstruct the file client-side. This maximises throughput for large downloads and also lets you retrieve just the header of a file (e.g., the first few bytes of a Parquet file) without fetching the whole object.

{{< qcm >}}
[
{
"question": "Which of the following statements about Amazon S3 bucket names is correct?",
"answers": [
{
"answer": "Bucket names must be unique within the same AWS account.",
"isCorrect": false,
"explanation": "Bucket names must be globally unique across ALL AWS accounts, not just within a single account."
},
{
"answer": "Bucket names must be globally unique across all AWS accounts.",
"isCorrect": true,
"explanation": "S3 bucket names share a global namespace. If any AWS account already owns a given name, no other account can create a bucket with that name."
},
{
"answer": "Bucket names must be unique within the same AWS region.",
"isCorrect": false,
"explanation": "The uniqueness constraint is global, not regional. A bucket name taken in one region cannot be reused in any other region by any account."
},
{
"answer": "A bucket can be moved to a different region after creation.",
"isCorrect": false,
"explanation": "A bucket's region is set at creation time and cannot be changed. You would have to create a new bucket in the target region and migrate the data."
}
]
},
{
"question": "A developer needs to uniquely identify a specific version of an object in Amazon S3. What combination of attributes achieves this?",
"answers": [
{
"answer": "Bucket name and object key",
"isCorrect": false,
"explanation": "Without a version ID, bucket name + key identifies only the latest version of an object, not a specific historical version."
},
{
"answer": "Bucket name, object key, and version ID",
"isCorrect": true,
"explanation": "The combination of bucket name + key + version ID uniquely identifies every object version stored in S3 when versioning is enabled."
},
{
"answer": "Object key and ETag",
"isCorrect": false,
"explanation": "An ETag is a hash of the object content and is not sufficient alone to uniquely locate an object — it does not include the bucket or version context."
},
{
"answer": "Bucket name, object key, and ETag",
"isCorrect": false,
"explanation": "The ETag is metadata about the object's content, not a locator. The correct unique identifier uses the version ID, not the ETag."
}
]
},
{
"question": "A company stores compliance documents that are accessed roughly once every three months and must be retrievable within milliseconds when needed. Which S3 storage class is the most cost-effective choice?",
"answers": [
{
"answer": "S3 Standard",
"isCorrect": false,
"explanation": "S3 Standard is optimised for frequently accessed data. Using it for quarterly-access data incurs unnecessary storage costs."
},
{
"answer": "S3 Glacier Flexible Retrieval",
"isCorrect": false,
"explanation": "Glacier Flexible Retrieval has retrieval times of minutes to hours, which does not meet the millisecond retrieval requirement."
},
{
"answer": "S3 Glacier Instant Retrieval",
"isCorrect": true,
"explanation": "Glacier Instant Retrieval is specifically designed for archive data accessed approximately once per quarter that still requires millisecond retrieval times."
},
{
"answer": "S3 Standard-IA",
"isCorrect": false,
"explanation": "Standard-IA is suited for data accessed less than once a month with a per-retrieval fee. For quarterly access needing archive pricing, Glacier Instant Retrieval is more cost-effective."
}
]
},
{
"question": "An S3 lifecycle policy is configured to transition objects from S3 Standard to S3 Glacier Flexible Retrieval after 90 days. A developer now wants to add a rule to move objects back from Glacier to Standard after 180 days. Is this possible?",
"answers": [
{
"answer": "Yes, lifecycle rules can transition objects in any direction between storage classes.",
"isCorrect": false,
"explanation": "Lifecycle transitions can only move objects down the cost hierarchy. Moving data back up (e.g., Glacier → Standard) requires a restore operation, not a lifecycle rule."
},
{
"answer": "No, lifecycle rules can only transition objects down the cost hierarchy, not back up.",
"isCorrect": true,
"explanation": "S3 lifecycle policies only support downward transitions (e.g., Standard → Standard-IA → Glacier). Restoring from Glacier to a higher-performance tier requires an explicit restore operation."
},
{
"answer": "Yes, but only if versioning is enabled on the bucket.",
"isCorrect": false,
"explanation": "Versioning does not enable upward lifecycle transitions. The restriction is inherent to how lifecycle rules work, regardless of versioning state."
},
{
"answer": "No, lifecycle rules cannot move objects between archive tiers at all.",
"isCorrect": false,
"explanation": "Lifecycle rules can transition between archive tiers in the downward direction (e.g., Glacier Instant → Glacier Flexible → Glacier Deep Archive). The restriction is specifically on moving back up."
}
]
},
{
"question": "Versioning is enabled on an S3 bucket. A developer issues a DELETE request on an existing object without specifying a version ID. What is the result?",
"answers": [
{
"answer": "The object and all its previous versions are permanently deleted.",
"isCorrect": false,
"explanation": "Without a version ID, a DELETE does not permanently remove any data. All versions remain intact."
},
{
"answer": "The latest version is permanently deleted, and the previous version becomes current.",
"isCorrect": false,
"explanation": "This would be the behaviour of a DELETE with a specific version ID. Without a version ID, no version is permanently deleted."
},
{
"answer": "A delete marker is placed on the object, making it appear deleted while all versions remain recoverable.",
"isCorrect": true,
"explanation": "In a versioned bucket, a DELETE without a version ID inserts a delete marker. The object appears deleted, but all prior versions are preserved and can be restored by removing the marker."
},
{
"answer": "The request is rejected because versioning prevents deletion.",
"isCorrect": false,
"explanation": "Versioning does not block DELETE requests. It simply places a delete marker instead of erasing data, preserving recovery options."
}
]
},
{
"question": "Which of the following are true about MFA Delete in Amazon S3? (Select TWO)",
"answers": [
{
"answer": "MFA Delete can be enabled by any IAM user with s3:PutBucketVersioning permission.",
"isCorrect": false,
"explanation": "MFA Delete can only be enabled by the bucket owner using the root account via the AWS CLI. It cannot be enabled by IAM users."
},
{
"answer": "MFA Delete requires a valid MFA token to permanently delete an object version.",
"isCorrect": true,
"explanation": "Once MFA Delete is enabled, permanently deleting a specific object version requires both valid AWS credentials and a valid MFA token."
},
{
"answer": "MFA Delete requires versioning to be enabled on the bucket.",
"isCorrect": true,
"explanation": "MFA Delete is an extension of versioning. Versioning must be active on the bucket before MFA Delete can be configured."
},
{
"answer": "MFA Delete can be configured through the AWS Management Console.",
"isCorrect": false,
"explanation": "MFA Delete must be enabled using the AWS CLI with root account credentials. The console does not support this operation."
}
]
},
{
"question": "A startup wants to host a React single-page application (SPA) without managing any servers. They plan to use S3 static website hosting. Which additional AWS service should they add to support HTTPS and a custom domain?",
"answers": [
{
"answer": "AWS Elastic Load Balancing",
"isCorrect": false,
"explanation": "ELB routes traffic to compute resources (EC2, containers). It is not the right tool to add HTTPS and a custom domain in front of an S3 static website."
},
{
"answer": "Amazon CloudFront",
"isCorrect": true,
"explanation": "CloudFront is the standard complement to S3 static website hosting. It provides HTTPS termination, global edge caching, and custom domain support, keeping the solution fully serverless."
},
{
"answer": "AWS API Gateway",
"isCorrect": false,
"explanation": "API Gateway fronts backend APIs, not static file buckets. It would add unnecessary complexity and cost for serving a static site."
},
{
"answer": "Amazon Route 53 alone",
"isCorrect": false,
"explanation": "Route 53 handles DNS but cannot itself provide HTTPS or caching. It would still need CloudFront to support HTTPS for an S3-hosted site."
}
]
},
{
"question": "A developer wants to allow an end user to upload a large file directly from their browser to a private S3 bucket without exposing AWS credentials to the client. What is the correct approach?",
"answers": [
{
"answer": "Make the S3 bucket public and have the user upload directly.",
"isCorrect": false,
"explanation": "Making the bucket public exposes all objects to anyone on the internet, which is a major security risk and not the recommended approach."
},
{
"answer": "Generate a pre-signed URL with the PUT method on the backend and provide it to the client for direct upload.",
"isCorrect": true,
"explanation": "A pre-signed PUT URL embeds the server's credentials and an expiry time. The client uses the URL to upload directly to S3 without needing its own AWS credentials, offloading bandwidth from the backend."
},
{
"answer": "Generate a pre-signed URL with the GET method on the backend and provide it to the client.",
"isCorrect": false,
"explanation": "A pre-signed GET URL grants download access, not upload access. You need a pre-signed PUT URL to allow a client to upload an object."
},
{
"answer": "Create a temporary IAM user for each upload and delete it after the upload completes.",
"isCorrect": false,
"explanation": "Creating and deleting IAM users per upload is operationally complex and not a standard or secure pattern. Pre-signed URLs are the correct solution for this use case."
}
]
},
{
"question": "Which of the following statements about S3 Cross-Region Replication (CRR) are correct? (Select TWO)",
"answers": [
{
"answer": "Versioning must be enabled on both the source and destination buckets.",
"isCorrect": true,
"explanation": "S3 Replication (both CRR and SRR) requires versioning to be active on both buckets before replication rules can be configured."
},
{
"answer": "Existing objects in the source bucket are automatically replicated when a replication rule is created.",
"isCorrect": false,
"explanation": "Replication only applies to objects written after the rule is created. Existing objects must be replicated manually using S3 Batch Replication."
},
{
"answer": "Delete markers are replicated to the destination bucket by default.",
"isCorrect": false,
"explanation": "Delete markers are not replicated by default. This behaviour must be explicitly enabled in the replication configuration."
},
{
"answer": "CRR can be used to reduce latency for geographically distributed users by keeping a copy of data closer to them.",
"isCorrect": true,
"explanation": "One of the primary use cases for CRR is placing object copies in regions closer to end users, reducing read latency for globally distributed applications."
}
]
},
{
"question": "A team has a bucket shared by multiple applications and finds the bucket policy is becoming unmanageably large. Which S3 feature should they use to simplify access management per application?",
"answers": [
{
"answer": "S3 Access Control Lists (ACLs)",
"isCorrect": false,
"explanation": "ACLs are a legacy mechanism with coarse-grained permissions. They do not offer per-application policy scoping and are generally discouraged by AWS."
},
{
"answer": "S3 Access Points",
"isCorrect": true,
"explanation": "Access Points let you create a named endpoint per use case, each with its own focused access policy. This replaces one complex bucket policy with multiple smaller, purpose-specific policies."
},
{
"answer": "S3 Intelligent-Tiering",
"isCorrect": false,
"explanation": "Intelligent-Tiering is a storage class that manages cost based on access patterns. It has nothing to do with access policy management."
},
{
"answer": "S3 Object Lock",
"isCorrect": false,
"explanation": "Object Lock enforces data immutability for compliance purposes. It does not address access policy complexity."
}
]
},
{
"question": "Which S3 encryption option provides an audit trail in AWS CloudTrail for every decryption event?",
"answers": [
{
"answer": "SSE-S3",
"isCorrect": false,
"explanation": "SSE-S3 uses keys managed entirely by AWS with no CloudTrail logging of individual decryption operations."
},
{
"answer": "SSE-KMS",
"isCorrect": true,
"explanation": "SSE-KMS uses AWS KMS Customer Master Keys. Every decryption (i.e., every S3 GET for an encrypted object) generates a CloudTrail log entry, providing a full audit trail."
},
{
"answer": "SSE-C",
"isCorrect": false,
"explanation": "SSE-C uses keys you provide with each request. AWS does not store or manage these keys, and there is no KMS-level CloudTrail audit trail for decryptions."
},
{
"answer": "Client-Side Encryption",
"isCorrect": false,
"explanation": "With client-side encryption, AWS S3 stores ciphertext and has no knowledge of the keys or decryption operations, so no CloudTrail decryption audit trail exists."
}
]
},
{
"question": "A developer is seeing throttling errors on S3 GET requests for objects encrypted with SSE-KMS. What is the most likely cause?",
"answers": [
{
"answer": "The S3 bucket has reached its maximum object count.",
"isCorrect": false,
"explanation": "S3 has no fixed object count limit per bucket. Object count is not the cause of SSE-KMS throttling."
},
{
"answer": "Each GET request calls KMS to decrypt, and high S3 traffic is hitting KMS API rate limits.",
"isCorrect": true,
"explanation": "With SSE-KMS, every object retrieval requires a KMS API call to decrypt. At high request rates, this can exhaust KMS API quotas, causing throttling errors."
},
{
"answer": "SSE-KMS cannot be used with high-throughput S3 workloads.",
"isCorrect": false,
"explanation": "SSE-KMS can be used at high throughput, but you must request KMS quota increases if your traffic exceeds default limits. The statement that it 'cannot' be used is incorrect."
},
{
"answer": "The encryption key has expired and must be rotated.",
"isCorrect": false,
"explanation": "KMS key rotation does not cause throttling errors. The issue described is specifically related to KMS API rate limits from high-volume S3 access."
}
]
},
{
"question": "A financial institution requires that certain regulatory records stored in S3 cannot be deleted or modified by anyone — including the root account — for a 7-year period. Which S3 feature and mode satisfies this requirement?",
"answers": [
{
"answer": "S3 Object Lock in Governance mode",
"isCorrect": false,
"explanation": "Governance mode allows users with special IAM permissions to override or remove the lock. It does not prevent the root account from deleting objects and therefore does not satisfy the strictest compliance requirements."
},
{
"answer": "S3 Object Lock in Compliance mode",
"isCorrect": true,
"explanation": "Compliance mode prevents anyone — including the root account — from deleting or altering locked objects during the retention period. This is the strongest protection and is designed for strict regulatory requirements."
},
{
"answer": "S3 Versioning with MFA Delete",
"isCorrect": false,
"explanation": "MFA Delete adds an additional authentication step but does not guarantee immutability for a defined retention period. A user with root access and MFA could still delete versions."
},
{
"answer": "S3 Glacier Vault Lock",
"isCorrect": false,
"explanation": "Glacier Vault Lock applies to Glacier Vaults specifically, not to standard S3 buckets. Object Lock is the correct feature for S3 buckets."
}
]
},
{
"question": "A web application hosted at `app.example.com` uses JavaScript to fetch images directly from `assets.s3.amazonaws.com`. Users are seeing browser errors blocking these requests. What should the developer configure?",
"answers": [
{
"answer": "A bucket policy allowing public read access to all objects.",
"isCorrect": false,
"explanation": "Making the bucket public may allow access but does not resolve the browser-level cross-origin restriction. CORS must be configured to allow the cross-origin JavaScript requests."
},
{
"answer": "CORS configuration on the S3 bucket specifying `app.example.com` as an allowed origin.",
"isCorrect": true,
"explanation": "Browsers block cross-origin requests by default. Configuring CORS on the S3 bucket to allow the app's origin instructs the browser that these cross-origin requests are permitted."
},
{
"answer": "An S3 Access Point scoped to the application's VPC.",
"isCorrect": false,
"explanation": "Access Points control network-level access, not cross-origin browser policies. CORS is the correct mechanism for this browser-level issue."
},
{
"answer": "Enable Transfer Acceleration on the S3 bucket.",
"isCorrect": false,
"explanation": "Transfer Acceleration improves upload performance via edge locations. It has no effect on CORS or cross-origin browser restrictions."
}
]
},
{
"question": "A data engineering team stores large CSV files in S3 and needs to retrieve only rows where a specific column matches a value. They want to minimize data transfer costs without setting up any infrastructure. Which S3 feature should they use?",
"answers": [
{
"answer": "Amazon Athena",
"isCorrect": false,
"explanation": "Athena is a good option for large-scale analytics but requires infrastructure setup (a database, table definitions) and is heavier-weight than needed for simple filtering of individual files."
},
{
"answer": "S3 Select",
"isCorrect": true,
"explanation": "S3 Select lets you push a SQL-like query directly to S3 to filter and return only the matching subset of an object. It requires no infrastructure and significantly reduces transfer costs compared to downloading the full file."
},
{
"answer": "S3 Byte-Range Fetches",
"isCorrect": false,
"explanation": "Byte-Range Fetches retrieve specific byte ranges of an object in parallel. They do not understand file formats like CSV and cannot filter rows by column value."
},
{
"answer": "S3 Intelligent-Tiering",
"isCorrect": false,
"explanation": "Intelligent-Tiering is a storage class for cost optimisation based on access patterns. It provides no data querying capability."
}
]
},
{
"question": "A developer is uploading a 10 GB file to S3. Which upload strategies are appropriate for this object size? (Select TWO)",
"answers": [
{
"answer": "Single PUT upload using the standard PutObject API",
"isCorrect": false,
"explanation": "S3 requires Multipart Upload for objects larger than 5 GB. A standard single PutObject call cannot upload a 10 GB file."
},
{
"answer": "Multipart Upload to split the file into parts and upload them in parallel",
"isCorrect": true,
"explanation": "Multipart Upload is required for objects over 5 GB and recommended for objects over 100 MB. It enables parallel uploads and allows retrying only failed parts."
},
{
"answer": "S3 Transfer Acceleration combined with Multipart Upload",
"isCorrect": true,
"explanation": "Transfer Acceleration routes uploads through CloudFront edge locations via the AWS backbone, improving throughput for large uploads from distant locations. It can be combined with Multipart Upload."
},
{
"answer": "S3 Select to push the upload logic to S3",
"isCorrect": false,
"explanation": "S3 Select is a query feature for filtering data within stored objects. It has no role in the upload process."
}
]
},
{
"question": "Which of the following correctly describes S3 Intelligent-Tiering?",
"answers": [
{
"answer": "It requires the user to manually specify which tier each object should use at upload time.",
"isCorrect": false,
"explanation": "Intelligent-Tiering automatically monitors access patterns and moves objects between tiers without any manual intervention."
},
{
"answer": "It automatically moves objects between access tiers based on changing access patterns and charges a small monthly monitoring fee per object.",
"isCorrect": true,
"explanation": "Intelligent-Tiering monitors each object's access frequency and shifts it between Frequent, Infrequent, Archive Instant, Archive, and Deep Archive tiers automatically. A small per-object monitoring fee applies."
},
{
"answer": "It is the cheapest storage class for all types of workloads.",
"isCorrect": false,
"explanation": "For predictable access patterns, choosing the specific storage class (Standard, Standard-IA, Glacier, etc.) directly is more cost-effective than paying the Intelligent-Tiering monitoring fee."
},
{
"answer": "It only moves objects between Standard and Standard-IA tiers.",
"isCorrect": false,
"explanation": "Intelligent-Tiering covers multiple tiers: Frequent Access, Infrequent Access, Archive Instant, Archive, and Deep Archive — not just Standard and Standard-IA."
}
]
},
{
"question": "An application needs to enforce that all data transmitted to an S3 bucket is encrypted in transit. How can a developer enforce this?",
"answers": [
{
"answer": "Enable SSE-S3 on the bucket.",
"isCorrect": false,
"explanation": "SSE-S3 encrypts data at rest, not in transit. It does not prevent unencrypted HTTP requests from being accepted."
},
{
"answer": "Add a bucket policy that denies requests where the `aws:SecureTransport` condition is false.",
"isCorrect": true,
"explanation": "A bucket policy with a Deny statement on `aws:SecureTransport: false` blocks any request that does not use HTTPS/TLS, enforcing encryption in transit."
},
{
"answer": "Enable Transfer Acceleration on the bucket.",
"isCorrect": false,
"explanation": "Transfer Acceleration improves upload performance by routing through edge locations. It does not enforce HTTPS or block unencrypted requests."
},
{
"answer": "Configure an S3 Access Point with a VPC restriction.",
"isCorrect": false,
"explanation": "A VPC-scoped Access Point restricts traffic to originate from within the VPC but does not specifically enforce HTTPS for all requests."
}
]
},
{
"question": "A company wants to configure S3 event notifications so that when an object is uploaded, multiple downstream systems (email, a queue, and a Lambda function) are notified simultaneously. Which S3 event notification destination enables this fan-out pattern?",
"answers": [
{
"answer": "Amazon SQS",
"isCorrect": false,
"explanation": "SQS delivers the event to a single queue for reliable processing. It does not natively fan out to multiple distinct destinations simultaneously."
},
{
"answer": "Amazon SNS",
"isCorrect": true,
"explanation": "SNS is designed for fan-out: a single S3 event notification to an SNS topic can be delivered simultaneously to multiple subscribers — email endpoints, SQS queues, Lambda functions, etc."
},
{
"answer": "AWS Lambda",
"isCorrect": false,
"explanation": "Lambda processes events directly but does not inherently fan out to multiple independent systems in the way SNS does."
},
{
"answer": "Amazon EventBridge",
"isCorrect": false,
"explanation": "EventBridge can receive S3 events and route them, but the course content identifies Lambda, SQS, and SNS as the three native S3 event notification destinations. SNS is the specific fan-out answer here."
}
]
},
{
"question": "A developer wants to allow a partner company (a separate AWS account) to access specific objects in their S3 bucket. What is the recommended approach according to AWS best practices?",
"answers": [
{
"answer": "Set a canned ACL of `public-read` on the relevant objects.",
"isCorrect": false,
"explanation": "A public-read ACL makes objects accessible to everyone on the internet, not just the partner account. This is far too permissive."
},
{
"answer": "Create an IAM user in the partner's account and share the access key with the bucket owner.",
"isCorrect": false,
"explanation": "Sharing IAM credentials between accounts is a security anti-pattern. Cross-account bucket policies or IAM roles are the correct approach."
},
{
"answer": "Use a bucket policy to grant access to the specific principal in the partner's AWS account.",
"isCorrect": true,
"explanation": "Bucket policies support cross-account access by specifying the partner account's ARN as the principal. AWS recommends bucket policies over ACLs for this use case."
},
{
"answer": "Enable S3 Same-Region Replication to copy objects to a bucket in the partner's account.",
"isCorrect": false,
"explanation": "SRR could technically put a copy in another account's bucket, but it is not the right solution for simply granting read access. A bucket policy is simpler and more appropriate."
}
]
}
]
{{< /qcm >}}