baseURL = 'https://cloudnugget.com/'
locale = 'en-us'
title = '🏠'
theme = 'hugo-book'

[params.sidebar]
showLevel = 1  # 1 = seulement les sections de premier niveau

[markup]
    [markup.goldmark]
        [markup.goldmark.renderer]
            unsafe = true